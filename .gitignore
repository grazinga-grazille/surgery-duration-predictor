# Raw data — never committed (confidential health data)
data/
*.xlsx
*.xls
*.csv

# Precomputed folder — data now stored in Streamlit Secrets, not committed
precomputed/

# Training script — requires raw data to run
train_and_save.py

# Jupyter notebooks and checkpoints
.ipynb_checkpoints/
*.ipynb

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Environment & secrets
.env
.env.*
.streamlit/secrets.toml
venv/
.venv/

# macOS
.DS_Store
~$*

# PDFs / reports
*.pdf

# Research outputs (local only)
research/
